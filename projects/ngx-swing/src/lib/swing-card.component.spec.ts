import { SwingCardComponent } from './swing-card.component';

describe('SwingCardComponent', () => {
  it('should create an instance', () => {
    // const directive = new SwingCardDirective();
    // expect(directive).toBeTruthy();
  });
});
